<uix:UIXUserControlBase
    x:Class="GameTimeNext.Core.Application.CreateImportPackage.Views.CreateImportPackageView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:uix="clr-namespace:UIX.ViewController.Engine.FrameworkElements.UserControls;assembly=UIX.ViewController.Engine"
    xmlns:uixprops="clr-namespace:UIX.ViewController.Engine.AttachedProperties;assembly=UIX.ViewController.Engine"
    HorizontalAlignment="Stretch"
    VerticalAlignment="Stretch"
    HorizontalContentAlignment="Stretch"
    VerticalContentAlignment="Stretch"
    d:DesignHeight="520"
    d:DesignWidth="980"
    mc:Ignorable="d">

    <Grid Margin="24,20,24,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="18" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Vertical">
            <TextBlock
                FontSize="24"
                FontWeight="Bold"
                Foreground="{DynamicResource Brush.TextPrimary}"
                Text="Create Import Package" />
            <TextBlock
                Margin="0,4,0,0"
                Foreground="{DynamicResource Brush.TextSecondary}"
                Text="Choose an import type and output location, then execute the import." />
        </StackPanel>

        <Border
            Grid.Row="2"
            Padding="18"
            Background="{DynamicResource Brush.Surface}"
            BorderBrush="{DynamicResource Brush.Border}"
            BorderThickness="1"
            CornerRadius="{StaticResource DefaultCornerRadius}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="14" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140" />
                    <ColumnDefinition Width="14" />
                    <ColumnDefinition Width="420" />
                </Grid.ColumnDefinitions>

                <TextBlock
                    Grid.Row="0"
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Foreground="{DynamicResource Brush.TextSecondary}"
                    Text="Export Type" />

                <ComboBox
                    x:Name="CmbExportType"
                    Grid.Row="0"
                    Grid.Column="2"
                    Height="36"
                    VerticalAlignment="Center"
                    uixprops:UIXAttachedProperty.AdditionalEntry="(All)"
                    uixprops:UIXAttachedProperty.Source="From=T1CTABD;Where=TXTYP:eT;Text=DESCR;Value=TXNUM"
                    SelectedIndex="0"
                    SelectedValue=""
                    SelectedValuePath="Tag" />

                <TextBlock
                    Grid.Row="2"
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Foreground="{DynamicResource Brush.TextSecondary}"
                    Text="Output Path" />

                <Grid Grid.Row="2" Grid.Column="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="8" />
                        <ColumnDefinition Width="38" />
                    </Grid.ColumnDefinitions>

                    <TextBox
                        x:Name="TxbOutputPath"
                        Grid.Column="0"
                        Height="36"
                        VerticalAlignment="Center" />

                    <Button
                        x:Name="BtnBrowseOutputPath"
                        Grid.Column="2"
                        Width="38"
                        Height="36"
                        ToolTip="Browse output folder">
                        <TextBlock
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            FontFamily="Segoe MDL2 Assets"
                            FontSize="14"
                            Text="&#xE838;" />
                    </Button>
                </Grid>
            </Grid>
        </Border>

        <Button
            x:Name="BtnExecute"
            Grid.Row="4"
            Width="148"
            Height="38"
            HorizontalAlignment="Left"
            IsDefault="True">
            <StackPanel Orientation="Horizontal">
                <TextBlock
                    Margin="0,0,6,0"
                    VerticalAlignment="Center"
                    FontFamily="Segoe MDL2 Assets"
                    Text="&#xE768;" />
                <TextBlock VerticalAlignment="Center" Text="Execute" />
            </StackPanel>
        </Button>
    </Grid>
</uix:UIXUserControlBase>
