<ui:GTNWindow
    x:Class="GameTimeNext.Core.Application.Profiles.Views.ProfilesSteamImportView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:GameTimeNext.Core.Application.Profiles.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="clr-namespace:GameTimeNext.Core.Framework.UI.Base"
    Title="Steam import"
    Width="800"
    Height="750"
    MinWidth="720"
    MinHeight="420"
    ShowMaximizeButton="False"
    ShowMinimizeButton="False"
    Style="{StaticResource GTNWindowStyle}"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="8" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="14" />
            <RowDefinition Height="*" />
            <RowDefinition Height="16" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock
                FontSize="20"
                FontWeight="SemiBold"
                Text="Steam import" />
            <TextBlock
                Margin="0,4,0,0"
                Opacity="0.75"
                Text="Search and select a game from your Steam library." />
        </StackPanel>

        <TextBlock
            Grid.Row="2"
            Opacity="0.8"
            Text="Search" />

        <TextBox
            x:Name="TxbSearch"
            Grid.Row="4"
            Height="36"
            VerticalContentAlignment="Center"
            ToolTip="Type to filter the list" />

        <DataGrid
            x:Name="dgSteamGames"
            Grid.Row="6"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            CanUserDeleteRows="False"
            CanUserResizeRows="False"
            HeadersVisibility="Column"
            IsReadOnly="True"
            ItemsSource="{Binding SteamGames}"
            SelectedItem="{Binding SelectedSteamGame}"
            SelectionMode="Single"
            SelectionUnit="FullRow">
            <DataGrid.Columns>
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding Name}"
                    Header="Name" />
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button
                x:Name="BtnCancel"
                Grid.Column="1"
                Width="110"
                Height="36"
                Content="Cancel"
                IsCancel="True" />

            <Button
                x:Name="BtnImport"
                Grid.Column="3"
                Width="140"
                Height="36"
                Content="Import"
                IsDefault="True" />
        </Grid>
    </Grid>
</ui:GTNWindow>